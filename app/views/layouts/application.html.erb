<!DOCTYPE html>
<html>
  <head>
    <title>EasyFood</title> <!-- Título da aplicação -->
    <meta name="viewport" content="width=device-width,initial-scale=1"> <!-- Configuração de viewport para responsividade -->
    <%= csrf_meta_tags %> <!-- Meta tags para proteção CSRF -->
    <%= csp_meta_tag %> <!-- Meta tags para proteção contra ataques de injeção de conteúdo -->

    <link rel="preconnect" href="https://fonts.googleapis.com"> <!-- Preconexão com o Google Fonts -->
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin> <!-- Preconexão com o Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Chela+One&family=Roboto:wght@300;400;700&display=swap" rel="stylesheet"> <!-- Importando fontes do Google Fonts -->

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css"> <!-- Importando ícones do Bootstrap Icons -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %> <!-- Importando o CSS do Bootstrap e outros estilos -->
    <%= javascript_importmap_tags %> <!-- Importando o JavaScript do Bootstrap e outros scripts -->
  </head>

  <body>
    <%= render 'layouts/navbar' %> <!-- Renderizando o navbar -->

    <main class="container mt-4">
      <%# Bloco para exibir mensagens de alerta e notice %>
      <% flash.each do |type, msg| %>
        <%# Mapeando nossos tipos de flash para as classes de alerta do Bootstrap %>
        <% type = (type == "alert") ? "danger" : "success" %>
        <div class="alert alert-<%= type %>" role="alert">
          <%= msg %>
        </div>
      <% end %>

      <%= yield %>
    </main>

    <%= render 'layouts/footer' %> <!-- Renderizando o footer -->
  </body>
</html>
